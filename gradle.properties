# `version` is the single source of truth for the Core plugin (SinceDungeon):
# it lands in paper-plugin.yml via processResources, names the jar, and is what
# .github/workflows/auto-release.yml reads when cutting a release. See AGENTS.md
# before bumping. Premium declares its own version in Premium/build.gradle.
version=1.7.0
